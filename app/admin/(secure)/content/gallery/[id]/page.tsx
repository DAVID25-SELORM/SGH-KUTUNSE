import{notFound}from"next/navigation";import{requireAdmin}from"@/lib/server/auth";import{adminDb}from"@/lib/server/firebase-admin";import{serialize}from"@/lib/server/admin-data";import{GalleryAlbumEditor}from"@/components/admin/GalleryAlbumEditor";import type{GalleryAlbumInput}from"@/lib/gallery";
export default async function Page({params}:{params:Promise<{id:string}>}){await requireAdmin("content");const{id}=await params;if(!/^[\w-]{1,128}$/.test(id))notFound();const doc=await adminDb.collection("gallery_albums").doc(id).get();if(!doc.exists)notFound();return <section><h1 className="mb-6 text-3xl font-semibold text-purple-deep">Edit gallery album</h1><GalleryAlbumEditor id={id} initial={serialize(doc.data()!) as GalleryAlbumInput}/></section>}
