import Link from "next/link";
import { requireAdmin } from "@/lib/server/auth";
export default async function Page(){await requireAdmin("content");const items=[["gallery","Gallery albums"],["doctors","Doctors"],["services","Services"],["articles","Health articles"],["insurance-partners","Insurance partners"],["site-settings","Site settings"]];return <section><h1 className="text-3xl font-semibold text-purple-deep">Website content</h1><p className="mt-3 text-text-body">Static public data remains the verified fallback. Firestore content only replaces it after approval and publication review.</p><div className="mt-6 grid gap-4 sm:grid-cols-2">{items.map(([slug,label])=><Link key={slug} href={`/admin/content/${slug}`} className="rounded-2xl bg-white p-6 font-semibold text-purple-deep shadow-sm">{label}</Link>)}</div></section>}
