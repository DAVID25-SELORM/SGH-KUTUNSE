"use client";import {useState} from "react";import {ADMIN_ROLES} from "@/lib/types/admin";export function CreateAdminForm(){const[m,setM]=useState("");async function submit(fd:FormData){const r=await fetch("/api/admin/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(fd))});const j=await r.json();setM(r.ok?"Administrator created. Send a password-reset invitation from Firebase Authentication.":j.message??"Could not create administrator.");if(r.ok)window.location.reload()}return <form action={submit} className="mt-6 grid gap-3 rounded-2xl bg-white p-5 shadow-sm"><h2 className="font-semibold">Create controlled admin account</h2><input name="displayName" required placeholder="Display name" className="rounded-xl border border-border-default p-3"/><input name="email" type="email" required placeholder="Approved staff email" className="rounded-xl border border-border-default p-3"/><select name="role" className="rounded-xl border border-border-default p-3">{ADMIN_ROLES.map(r=><option key={r}>{r}</option>)}</select><button className="rounded-xl bg-purple-deep p-3 font-semibold text-white">Create account</button>{m&&<p className="text-sm">{m}</p>}</form>}

